@model IEnumerable<ERP_Compact.Models.CompanyViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row" id="messageDiv">
    <div class="col-lg-12">
        <p class='text-center @TempData["message_background"]'><b>@TempData["message_text"]</b></p>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <p>
            <a class="btn btn-primary" href="/MgtCompany/Create">Add New Company</a>
        </p>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <h2>Company List</h2>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <table class="table" id="dataTable">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.CompanyName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CompanyPhone)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CompanyAddress)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CompanyEmail)
                    </th>
                    <th></th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
            {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.CompanyName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CompanyPhone)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CompanyAddress)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CompanyEmail)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.CompanyKey }) |
                            @Html.ActionLink("Details", "Details", new { id = item.CompanyKey }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.CompanyKey }, new { onclick = "return confirm('Are you sure?');" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


<link href="~/Content/datatables/dataTables.bootstrap.css" rel="stylesheet" />
<script src="~/Scripts/jquery.dataTables.js"></script>
<script src="~/Scripts/dataTables.bootstrap.js"></script>
<script>
    $(function () {
        showMessageDiv();
        $("#dataTable").DataTable();
    });
    function showMessageDiv() {
        $("#messageDiv").hide();
        var message = '@TempData["message_text"]';
        if (message != '') {
            $("#messageDiv").show();
            setTimeout(hideMessageDiv, 2000);
        } 
    }
    function hideMessageDiv() {
        $("#messageDiv").hide(1000);
    }
</script>